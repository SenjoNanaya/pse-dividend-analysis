# Leave unset or empty for same-origin /api (local Vite proxy, Django SPA, Cloudflare Tunnel).
# For split hosting (Vercel UI + remote API), set the API origin with no trailing slash:
# VITE_API_BASE=https://your-api.onrender.com
VITE_API_BASE=
