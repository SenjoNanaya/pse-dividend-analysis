# Frontend

Vite + React dashboard for the PSE Edge analysis app.

See the [root README](../README.md) for setup, API endpoints, and how the registry / compare / report UI fits into the full pipeline.

```bash
# From repo root — after Django is running on :8000
cd frontend
npm install
npm run dev
```
